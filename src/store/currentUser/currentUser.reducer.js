const initialState = {
  email: '',
  name: '',
  user_id: '',
  logged: false,
};

const currentUser = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default currentUser;
